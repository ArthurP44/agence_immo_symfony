{% extends 'base.html.twig' %}
{% block title "Agence Lorem ipsum" %}

{%  block body %}
    <div class ="jumbotron text-center">
        <h1>Agence Lorem ipsum</h1>
        <p>Nulla dignissim quam a lorem bibendum bibendum. Integer pretium vulputate nibh, non gravida dui porttitor sed. Sed ultrices eleifend turpis, nec porta turpis venenatis vitae.</p>
    </div>
    <div class = "ml-5">
        <h2>les derniers biens :</h2>
        <div class="row flex">
            {%  for property in properties %}
                 <div class ="col-3">
                     <div class ="card">
                        <div class ="card-body">
                            <h5 class="card-title">
                                <a href="{{ path('property.show', {id: property.id, slug: property.slug}) }}">{{ property.title }}</a>
                            </h5>
                            <p class="card-text">{{ property.city }}({{ property.postalCode }})</p>
                            <div class ="text-primary" style="font-weight: bold; font-size: 2rem;">{{ property.formattedPrice }} €</div>
                        </div>
                     </div>
                 </div>
            {% endfor %}
        </div>
    </div>
{% endblock %}